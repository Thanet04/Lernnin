// package com.leanning.leanning.Repository;

// import org.springframework.data.jpa.repository.JpaRepository;
// import com.leanning.leanning.Model.Product; // เปลี่ยนเส้นทางตามที่เหมาะสม

// public interface ProductRepository extends JpaRepository<Product, Long> {
//     // คุณสามารถกำหนดเมธอดเพิ่มเติมสำหรับการค้นหาข้อมูลได้ที่นี่
//     List<Product> findByCategory(String category); // ตัวอย่างการค้นหาผลิตภัณฑ์ตามหมวดหมู่
// }
